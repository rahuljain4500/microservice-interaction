server:
  port: 8081

spring:
  application:
    name: app1

eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_SERVER_URL:http://localhost:8761}/eureka
  instance:
    prefer-ip-address: true
